# TODO
- define my own types for units (even if it means just typedef'ing them)
- make "parent" makefile and then individual ones for each component
- typedef or return type called STATUS? return SUCCESS or FAILURE
- refactor EVERYTHING
- remove std::cout
- filters need to be optimized so badly (especially FIR)
- break up subcomponents into their own libs that can be compiled