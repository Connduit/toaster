#ifndef TOASTER_TYPES_H
#define TOASTER_TYPES_H

#include <complex>
#include <vector>

typedef std::vector<std::complex<float>> IQData;

typedef std::vector<float> AudioData;

// TODO: enums for where we want to send iqdata (audio, visual, etc)

#endif