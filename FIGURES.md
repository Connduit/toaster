# Figures

### Flowchart

    main.cpp ---> main()
        // initializes signalHandler for Ctrl+C interrupt
        Toaster toaster;
        toaster.createToasterSubsystem()
        Toaster.startToasterSubsystem()
